!include src/assets/file/applications/espresso/5.2.1/pw.x/stdin.yml

# If we need to override any keys, we may use the following syntax:
# control: !include src/assets/file/applications/espresso/5.2.1/pw.x/stdin.yml#/control
# kv_pair:
#   regex: '(\w+)\s*=\s*([^,\n/=]+)'
#   flags: ["g", "i", "m"]
